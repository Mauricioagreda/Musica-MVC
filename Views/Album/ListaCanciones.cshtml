@model List<Musica_MVC.Models.Cancion>;

<div class="row">
    <div class="col-sm-8 offset-sm-2">
        <h2 class="text-center">Lista Canciones de 
            <span class="uwu">@Model.FirstOrDefault()?.Album?.Nombre</span>
        </h2>
        <p></p>
        <p></p>
        <p></p>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Duracion</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.IdCancion</td>
                        <td>@item.Nombre</td>
                        <td>@item.Duracion</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>